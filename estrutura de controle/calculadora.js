// Crie uma calculadora simples que opera com dois números e um operador (+, -, *, /).

const prompt = require('prompt-sync')();

function calc (){
  const numero = prompt("Digite o primeiro numero para somar")
  const numero1 = prompt("Digite o segundo número.")
  return parseFloat[numero,numero1];
};


const soma = calc();
if(true){
  soma = numero + numero1;
  return console.log(soma + "resultado das duas somas.")
};

calc();